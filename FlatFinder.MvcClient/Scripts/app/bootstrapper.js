define('bootstrapper',
    ['jquery', 'config', 'dataLoader'],
    function ($, config, dataLoader) {
        var
            run = function () {
            };

        return {
            run: run
        };
    });